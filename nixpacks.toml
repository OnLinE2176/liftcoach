[phases.setup]
aptPkgs = [
    "libgl1",
    "libglib2.0-0",
    "libxcb1",
    "libxext6",
    "libsm6",
    "libxrender1",
    "libfontconfig1",
    "libice6"
]

[start]
cmd = "streamlit run app_mediapipe.py --server.port ${PORT:-8501} --server.address 0.0.0.0"
